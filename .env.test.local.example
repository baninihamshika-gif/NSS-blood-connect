# Used ONLY by the RLS test suite (tests/) to create/confirm/delete disposable
# test users via the Supabase Admin API. Get this from Project Settings > API >
# service_role key. NEVER used by application code, NEVER put in .env.
SUPABASE_SERVICE_ROLE_KEY=
